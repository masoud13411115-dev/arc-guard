kind = "web"
previewPath = "/"
title = "ARCtime"
version = "1.0.0"
id = "artifacts/arctime"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19585

[services.development]
run = "pnpm --filter @workspace/arctime run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/arctime", "run", "build" ]
publicDir = "artifacts/arctime/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19585"
BASE_PATH = "/"
